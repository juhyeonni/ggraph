{
  "name": "ggraph",
  "version": "1.1.1",
  "description": "Commit graph on GitHub's commits page",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "postinstall": "wxt prepare",
    "test": "vitest run --passWithNoTests",
    "test:e2e": "wxt build && playwright test",
    "bench": "node benchmarks/layout-bench.mjs",
    "bench:bundle-size": "node benchmarks/bundle-size.mjs",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@playwright/test": "^1.61.1",
    "fs-extra": "^11.3.6",
    "happy-dom": "^20.11.0",
    "js-yaml": "^5.2.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wxt": "^0.20.27"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
